export interface InstituteResponse {
  id: number;
  bankCode: string;
  name: string;
  website: string;
  lastModifiedDate: string;
  // branches: Branch[];
  // createdDate: string;
  // version: number;
}
